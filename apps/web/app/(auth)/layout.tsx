// Pass-through layout — AuthShell provides the actual visual structure.
// Route group exists to keep auth pages logically grouped.
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return children;
}
