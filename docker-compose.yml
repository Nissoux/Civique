services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: civique
      POSTGRES_PASSWORD: civique
      POSTGRES_DB: civique
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
